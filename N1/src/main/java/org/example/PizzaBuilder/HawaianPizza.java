package org.example.PizzaBuilder;

import java.util.List;

public class HawaianPizza extends Pizza {
    public HawaianPizza(Size size, Dough doughType, List<String> toppings) {
        super(size, doughType, toppings);
    }
}
